#include "calc.h"
#include <stdio.h>
////
int main()
{
    int a = 5, b = 3;
    printf("a = %d\n", a);
    printf("b = %d\n", b);
    printf("add(a,b) = %d\n", add(a, b));
    printf("sub(a,b) = %d\n", sub(a, b));
    return 0;
}
